/*let cardHeader, cardContent,  cardIconShow;

cardHeader = document.querySelectorAll('.c-cardQuestions__header')
cardContent = document.querySelectorAll('.c-cardQuestions__content')
cardIconShow = document.querySelectorAll('.cardIconShow')
let count = 0

function cardEffect(event) {
   cardHeaderSelected = event.target.parentNode.parentNode
   cardHeaderSelected.toggle('teste')
   
}

cardIconShow.forEach((element,index) => {
    element.addEventListener('click', cardEffect)
})*/